apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
spec:
  selector:
    app: messaging-app
    version: v2 # This points to Blue initially
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
