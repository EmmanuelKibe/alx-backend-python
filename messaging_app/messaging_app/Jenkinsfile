pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', 
                    credentialsId: 'github-creds', 
                    url: 'https://github.com/EmmanuelKibe/alx-backend-python'
            }
        }
        stage('Test') {
            steps {
                // This block uses the ShiningPanda plugin
                // 'Python3' must match the name you gave in Tools
                pythonVirtualenv(pythonHome: 'Python3') {
                    sh 'pip install -r requirements.txt'
                    sh 'pip install pytest pytest-django'
                    sh 'pytest --junitxml=test-reports/results.xml'
                }
            }
        }
    }
    post {
        always {
            junit 'test-reports/results.xml'
        }
    }
}